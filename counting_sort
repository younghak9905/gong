#include <stdio.h> 
#define MAX_NUM 10000

int main() {
	int n;
	int a=0;
	int count[MAX_NUM + 1] = { 0, };
	scanf("%d", &n);
	for (int i = 1;i<= n;i++)
	{
		scanf("%d", &a);
		count[a]++;
	}
	
	for (int i = 1;i <= MAX_NUM;i++)
	{
		if (count[i]!=0)
		{
			for (int j = 0;j < count[i];j++)
				printf("%d\n", i);
		}
	}

}
	

